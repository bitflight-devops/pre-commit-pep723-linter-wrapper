# GitLab Personal Access Token for package publishing
# Create at: https://sourcery.assaabloy.net/-/profile/personal_access_tokens
# Required scopes: api, write_repository
GITLAB_TOKEN=your_token_here

# Build configuration
BUILD_TYPE=Release
NUM_JOBS=4
VERBOSE=0

# Container runtime (docker or podman)
# Leave unset for automatic detection
# CONTAINER_RUNTIME=docker

# ==============================================================================
# USB Hardware Testing Configuration
# ==============================================================================
# These environment variables enable real USB hardware tests.
# Tests skip gracefully when not set - no special CI configuration needed.
#
# To find your device NODE values, run:
#   hubpower introspect --human
#
# The NODE column shows stable identifiers like "3-1" or "3-1.1"
# These paths represent physical USB port locations and don't change
# across reboots (as long as device stays in same physical port).

# USB Hub for testing hub operations
# Example from introspect output:
#   Bus 003 Device 024  3-1        USB2.1 Hub (0bda:5411)
#                       ^^^
# USB_TEST_HUB_NODE=3-1

# USB Device for testing reset and device operations
# Example from introspect output:
#   Bus 003 Device 030  3-1.1      i2c_analyzer (2e8a:000a)  [ttyACM0]
#                       ^^^^^
# USB_TEST_DEVICE_NODE=3-1.1

# How tests use these values:
# - If set: Tests run with real hardware
# - If not set: Tests skip with helpful messages
# - GitLab CI: Set in runner config.toml to enable on hardware runners
# - Local dev: Set in .env file (gitignored) for your setup
